export function render() {
  // ------------------------------------------------------------------------------------------
  // [meta]
  // ------------------------------------------------------------------------------------------
  // 내용: Web API 설계 패턴 읽는 법의 전체 지도를 잡는다.
  // 링크: https://zeno.it.kr/javascript/web_apis/02_api-design-patterns
  // 배우고자 하는 것:
  // - Web API 문서를 읽을 때 어떤 순서로 보면 좋은지 확인한다.
  // - 이번 02편에서 나눠서 볼 주제를 정리한다.
  // ------------------------------------------------------------------------------------------

  // ------------------------------------------------------------------------------------------
  // [용어정리]
  // - Web API: 브라우저가 JavaScript에 추가로 제공하는 기능
  // - Web IDL: Web API의 속성, 메서드, 인자 모양을 적어둔 설계 문법
  // - Interface: API 객체의 공통 설계 단위
  // - Dictionary: 옵션 객체의 모양을 정의하는 스펙 용어
  // - Feature Detection: 브라우저 이름이 아니라 기능 존재 여부를 직접 확인하는 방식
  // ------------------------------------------------------------------------------------------

  console.log("Web API 설계 패턴 전체 지도");

  // ------------------------------------------------------------------------------------------
  // [1]
  // ------------------------------------------------------------------------------------------
  // 내용: 02편을 작은 주제로 나눠서 본다.
  //
  // 결과:
  // 02편에서 공부할 하위 주제 목록이 표로 출력된다.
  // ------------------------------------------------------------------------------------------

  const topics = [
    {
      file: "02",
      title: "전체 지도",
      point: "Web API 설계 패턴에서 무엇을 볼지 먼저 잡는다."
    },
    {
      file: "02-1",
      title: "AbortController와 Web IDL",
      point: "readonly, signal, abort 같은 스펙 표현이 실제 객체에서 어떻게 보이는지 확인한다."
    },
    {
      file: "02-2",
      title: "옵션 딕셔너리",
      point: "once 같은 옵션 값이 boolean으로 변환되는 함정을 확인한다."
    },
    {
      file: "02-3",
      title: "인터페이스 상속",
      point: "button, input, MouseEvent가 어떤 기능을 물려받는지 확인한다."
    },
    {
      file: "02-4",
      title: "이벤트 리스너 스펙 규칙",
      point: "addEventListener의 중복 등록 기준과 signal 자동 제거를 확인한다."
    },
    {
      file: "02-5",
      title: "기능 탐지와 스펙 용어",
      point: "in, MUST, SHOULD, MAY, MDN에서 스펙으로 가는 흐름을 정리한다."
    },
    {
      file: "02-6",
      title: "crypto와 바이트 데이터",
      point: "crypto, UUID, 랜덤 값, TextEncoder, SHA-256 해시를 눈으로 확인한다."
    }
  ];

  console.table(topics);

  // ------------------------------------------------------------------------------------------
  // [2]
  // ------------------------------------------------------------------------------------------
  // 내용: MDN과 스펙의 역할을 구분한다.
  //
  // 결과:
  // MDN과 스펙의 차이가 출력된다.
  // ------------------------------------------------------------------------------------------

  console.log("MDN: 사용법과 예제를 먼저 보는 문서");
  console.log("Spec: 브라우저가 실제로 어떻게 구현해야 하는지 적힌 설계도");
}

// ------------------------------------------------------------------------------------------
// [배운 것]
// - Web API 문서는 한 번에 다 외우는 것이 아니라 패턴별로 쪼개서 읽는다.
// - Web IDL은 API의 겉모양을 알려주는 설계 문법이다.
// - MDN은 사용 설명서에 가깝고, 스펙은 구현 설계도에 가깝다.
// - 애매한 동작은 MDN에서 시작해서 스펙 알고리즘 단계까지 내려가 확인한다.
// ------------------------------------------------------------------------------------------
