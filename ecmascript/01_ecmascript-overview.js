export function render() {
	// --------------------------------------------------------------------------------
	// [링크] https://zeno.it.kr/javascript/ECMAscript/01_ecmascript-overview
	// [주제] ECMASCript를 읽는 법:표준, 용어, 버전
	// [날짜] 2026-09-10
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [ECMAScript]
	// --------------------------------------------------------------------------------
	// European Computer Manufacturers Association(유럽 컴퓨터 제조사 협회)
	// 지금은 Ecma International
	// Script : 컴파일 없이 위에서부터 읽어 실행하는 코드
	// ECMAScript는 규격(표준문서), Javascript는 ㄱ ㅡ규격을 따르는 언어의 통칭,제품명
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [표준,구현,엔진]
	// --------------------------------------------------------------------------------
	// ECMA-262명세 - 표준 (규격 문서)
	// V8 - Chrome, Node.js 엔진
	// SpiderMonkey - Firefox 엔진
	// JavaScriptCore - Safari 엔진
	// 호스트: 브라우저/Node.js
	// 우리가 쓰느 JavaScript 코드
	// 표준(standard): 이 코드는 이런 결과를 내야한다고 글로 적힌 약속(실행x)
	// 구현(implementation) : 그 약속을 실제로 지키도록 만든 프로그램(엔진)
	// 호스트 환경(host environment): 엔진을 품고 있는 바깥 프로그램, 브라우저, Node.js, Deno
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [실습]
	// --------------------------------------------------------------------------------
	// 1. ECMAScript 표준이 정의하는 것 - 어느 엔진에서든 존재해야 한다
	console.log("표준",typeof Object, typeof Array, typeof Promise, typeof Symbol);

	// 2. 호스트 환경(브라우저)이 얹어준 것 - 표준 문서에는 없다
	console.log("호스트",typeof console);
	console.log("호스트",typeof document);
	console.log("호스트",typeof setTimeout);

	// 3. 전역 객체의 표준 이름은 globalThis 하나뿐이다.
	console.log("globalThis === window ?", globalThis === window);

	// 4. 컴퓨터는 모든 숫자를 0과 1로 이루어진 이진수로 변환해 계산합니다. 그런데 십진수 0.1과 0.2를 이진수로 바꾸면 무한 소수(끝없이 반복되는 수)가 됩니다
	// Number.MAX_SAFE_INTEGER (안전한 최대 정수)
	console.log("0.1+0.2 =",0.1+0.2);
	console.log("Number.MAX_SAFE_INTGER=",Number.MAX_SAFE_INTEGER)

	// 실험값
	const 실험값 = 0.1 + 0.2;
	console.log("오차가있나?",실험값 ===0.3);
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [매월 6월에 나오는 표준]
	// --------------------------------------------------------------------------------
	// ES5 - strict mode = 엄격모드
	// ES5 - Object.defineProperty - 객체 내부의 속성(프로퍼티)을 아주 세밀하게 제어하는 기능
	// ES6 - let,const - 기존 느슨했던 변수 선언 var 의 단점(재선언, 스코프 꼬임) 해결하기 위해
	// ES6 - Arrow Function - 코드간결, 내부 this 고정되어 헷갈리던 thsi 바인딩 문제
	// ES6 - class - 객체 지향 프로그래밍을 더 쉽게 할 수 있또록
	// ES6 - Promise - 비동기 처리를 깔끔하게 관리
	// ES6 - import / export - 코드를 여러 파일로 분리 조립
	// ES6 - Symbol - 절대 중복되지 않는 고유한 값을 만들어내는 새로운 원시 타입
	// ES6 - iterator - 반복 가능한 객체(배열등)순회할 수 있는 표준 약속(규격)
	// ES6 - ** - 지수연산자
	// ES6 - Array.prototype.includes - 배열 안ㅇ ㅔ특정 값이 들어있는지 확인하는 메서드
	// ES2017 - async / await - ES6 promise 한 단계 더 발전
	// ES2017 - Object.entries - 객체의 {키:값}쌍들을 배열모양 [[키,값],[키,값]]으로 변환
	// ES2018 - 객체 스프레드(...) - 점 세 개(...)를 이용ㅎ ㅐ객체의 내용을 그대로 펼침
	// ES2018 - Promise.prototype.finally - 비동기 작업이 성공하든 실패하든 상관없이 무조건 마지막에 실행
	// ES2018 - Array.prototype.flat - 중첩된 배열(배열 안의 배열)을 평평하게 펴주는 메서드
	// ES2020 - 옵셔널 체이닝(?.) - 에러 없이 안전학 ㅔ객체 속성에 접근하는 문법.
	// ES2020 - 널 병합 연산자(??) - 값이 비어있을 떄 (null 또는 undefined일 떄)의 기본값을 지정 - 기존 ||와 달리 숫자 0이나 빈 문자열""은 정상으로 인정
	// ES2020 - BigInt - Number.MAX_SAFE_INTEGER를 넘어가는 엄청나게 큰 정수 다룰 수 있는 타입. 숫자 뒤에 n을 붙여서 표현
	// ES2020 - globalThis - 브라우저든 서버든 환경 따지지 않고 전역 객체 접근
	// ES2021~ - 논리 할당 연산자 (&&=, ||=, ??=) - 논리 연산과 변수 대입을 합친 문법 - a??= b : a가 비어있으면 b를 대입하라
	// ES2021~ - Array.prototype.at() - 배열 특정 위치 요소를 가져오는 대괄호 []의 발전형 - arr.at(-1): 배열의 가장 마지막 요소
	// ES2021~ - 클래스 private 필드(#) - 클래스 내부 변수 이름 앞에 #을 붙이면 외부에서 절대로 건드릴 수 없는 비밀 변수가 됨
	// ES2021~ - Object.groupBy - 배열 안의 데이터들을 특정 기준에 따라 그룹별로 묶어서 객체로 재분류해 주는 유용한 함수
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [추상연산 (Abstract Operation)]
	// --------------------------------------------------------------------------------
	// ToNumber, ToPrimitive처럼 코드로는 직접 부를 수 없고 명세서 안에서만 쓰이는 내부 함수
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [ 내부 슬롯과 내부 메서드 ([[  ]] 이중 대괄호)]
	// --------------------------------------------------------------------------------
	// [[Prototype]],[[Value]]처럼 자바스크립트가 내부적으로 데이터를 보관하거나 움직일 떄 쓰는 숨겨진 칸/기능
	// Object.getPrototypeOf() 같은 안전한 간접 통로를 통해서만 내부를 살짝 들여다볼 수 있게 허락해 줍니다.
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [알고리즘 단계]
	// --------------------------------------------------------------------------------
	// ToBoolean(argument)의 1단게~6단계까지의 조건문 목록.
	// 자바스크립트에서 어떤 값이 참(true)인지 거짓(false)인지 판가름할 때, 엔진은 명세서에 적힌 번호 순서대로 검사를 진행합니다.
	// "너 undefined니? 그럼 거짓(false) 탈락!" (2단계)
	// "너 글자(String)니? 근데 빈 글자야? 그럼 거짓(false) 탈락! 글자가 들어있으면 합격(true)!"
	// "너 배열이나 객체(Object)니? 비어있든 말든 객체는 무조건 프리패스 합격(true)!"
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [실습2]
	// --------------------------------------------------------------------------------
	// 자바스크립트 내부에 존재하는 공식 거짓(false) 판별 기준 6가
	function 내가만든ToBoolean(값) {
		// 1. 원래 참/거짓이면 그대로 뱉기
		if (typeof 값 === "boolean") return 값;

		// 2. undefined는 거짓(false)!
		if(값=== undefined) return false;

		// 3. null도 거짓(false)!
		if(값=== null) return false;

		if(typeof 값 === "number"){
			// 4. 숫자인데 0이거나 NaN(숫자가 아님)이면 거짓(false)! 나머진 참!
			if (값 === 0 || Number.isNaN(값)) return false;
			return true
		}

		if(typeof 값 === "string"){
			// 5. 글자 수가 0개(빈 문자열 "")면 거짓(false)! 글자가 하나라도 있으면 참!
			return 값.length !==0;
		}

		// 6. 그 외에 배열[], 객체{}, 함수 등은 무조건 참(true)!
		return true;

	}


	const 검사대상 = [
		// 기본값들
		true,false,undefined,null
		// 다양한 숫자들
		,0,-0,NaN,1,-1,Infinity,
		// 다양한 문자열들 (빈 칸 포함)
		"","0","false"," ",
		// 객체, 배열, 함수
		{},[],function(){}
	];

	// 시험지에 적힌 값들을 하나씩 꺼내서 진짜 자바스크립트의 정답(Boolean(값))과
	// 내가 만든 채점기의 정답(내가만든ToBoolean(값))을 일일이 비교해 보는 과정
	검사대상.forEach(function(값){
		// 1. 진짜 자바스크립트가 채점한 결과
		const 표준결과 = Boolean(값);
		// 2. 내가 만든 수동 채점기가 채점한 결과
		const 내결과 = 내가만든ToBoolean(값);
		// 화면에 이쁘게 출력하기 위해 글자는 따옴표 붙여주는 코드
		const 표기 = typeof 값 ==="string" ? JSON.stringify(값) : String(값);
		// 3. 두 채점기의 결과가 똑같으면 "일치", 하나라도 틀리면 "불일치!"를 화면에 찍기
		console.log(표기,"->",내결과,표준결과 === 내결과 ? "일치" : "불일치!");
	});

	// 예상 결과
	// true -> true 일치
	// false -> false 일치
	// undefined -> false 일치
	// null -> false 일치
	// 0 -> false 일치
	// -0 -> false 일치
	// NaN -> false 일치
	// 1 -> true 일치
	// -1 -> true 일치
	// Infinity -> true 일치
	// "" -> false 일치      <-- (빈 문자열은 5단계에 의해 거짓!)
	// "0" -> true 일치     <-- (글자 수가 1개 있어서 참!)
	// "false" -> true 일치 <-- (글자 수가 5개 있어서 참!)
	// " " -> true 일치     <-- (공백 글자가 1개 있어서 참!)
	// {} -> true 일치      <-- (빈 객체는 6단계에 의해 무조건 참!)
	// [] -> true 일치      <-- (빈 배열도 객체이므로 무조건 참!)
	// function() {} -> true 일치

	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [JSON.stringify와 String은 뭐가 다를까]
	// --------------------------------------------------------------------------------
	// JSON.stringify(값) (문자열에 따옴표 붙이기) -  들어온 값이 '원래 문자열'일 때만 무조건 따옴표를 붙이고, 글자가 아닌 값(숫자, 불리언 등)은 따옴표를 붙이지 않는다
	// String(값) (나머지 값들을 안전하게 글자로 바꾸기) - 글자가 아닌 것은 순수한 글자로 만들고, 이미 글자인 것은 겉의 따옴표를 벗겨서 알맹이 글자만 보여준다
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [JSON.stringify가 가진 치명적인 약점]
	// --------------------------------------------------------------------------------
	// JSON 규격에는 자바스크립트의 undefined나 function이라는 개념이 존재하지 않습니다.
	// JSON.stringify에 이 값들을 넣으면 아래와 같이 데이터가 아예 사라지거나 먹통이 되는 현상이 발생합니다.
	// undefined를 넣었을 때 - JSON.stringify(undefined) 결과는 글자가 아니라 아예 아무 값도 없는 undefined(진짜 빈손)를 반환합니다.
	//  함수(function(){})를 넣었을 때 - JSON.stringify(function(){}) 이것 역시 결과가 undefined가 되어 함수 내용이 통째로 증발해 버립니다.
	// --------------------------------------------------------------------------------

	// --------------------------------------------------------------------------------
	// [마무리: 이번에 완벽하게 배운 내용 총정리]
	// --------------------------------------------------------------------------------
	// 1. ECMAScript 표준과 자바스크립트 엔진의 관계
	//    - ECMAScript는 공식 규칙 책(도면)이고, 자바스크립트 엔진은 이를 실행하는 프로그램입니다.
	//    - 개발자가 자바스크립트 코드를 실행하면 엔진은 규칙 책에 적힌 대로 움직입니다.

	// 2. 내부 슬롯과 추상 연산의 차이
	//    - 내부 슬롯([[Prototype]] 등)은 숨겨진 금고 같아서 간접 통로(Object.getPrototypeOf)로 구경할 수 있습니다.
	//    - 추상 연산(ToBoolean, ToString 등)은 비공개 내부 함수라서 자바스크립트 코드로 절대 볼 수 없습니다.
	//    - 따라서 추상 연산이 어떻게 작동하는지 아는 유유한 방법은 표준 명세서 문서를 읽는 것뿐입니다.

	// 3. 자바스크립트의 독특한 참/거짓(ToBoolean) 채점 기준
	//    - 어떤 값이 참(true)이고 거짓(false)인지는 명세서의 ToBoolean 알고리즘 순서대로 결정됩니다.
	//    - 문자열 "0"은 '글자 수가 1개 존재하므로' 참(true)이 됩니다.
	//    - 빈 배열 []은 '배열(객체)은 내용물과 상관없이 무조건 프리패스하므로' 참(true)이 됩니다.

	// 4. 안전한 화면 출력을 위한 삼항 연산자 분기 이유
	//    - 숫자 0과 문자열 "0"을 눈으로 구분하기 위해, 원래 문자열인 값에만 JSON.stringify를 썼습니다.
	//    - JSON.stringify는 문자열 데이터에 진짜 눈에 보이는 따옴표("")를 강제로 입혀서 내보냅니다.
	//    - 반면 String() 함수는 따옴표를 새로 붙이지 않고 값의 이름 그대로를 순수한 글자로 바꿉니다.

	// 5. JSON.stringify의 치명적인 약점 방어
	//    - JSON.stringify에 undefined나 함수(function)를 넣으면 데이터가 글자로 변하지 않고 아예 증발(undefined 반환)합니다.
	//    - 이 지뢰들을 안전하게 처리하기 위해, 삼항 연산자를 써서 문자열만 골라내고 나머지는 String()으로 처리한 것입니다.
	// --------------------------------------------------------------------------------

}

